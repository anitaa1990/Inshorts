<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true" android:color="@color/color_offline" />
    <item android:state_enabled="false" android:color="@color/color_offline" />
    <!-- pressed -->
    <item android:state_focused="true" android:color="@color/color_offline" />
    <!-- focused -->
    <item android:state_selected="true" android:color="@color/color_offline" />
    <!-- selected -->
    <item android:color="@color/toolbar_text_color" />
    <!-- default -->
</selector>